# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local build/dev artifacts (not part of the repo)
# WSL ALSA build workaround: libasound2-dev .deb extracted manually for cpal
*.deb

# Local-only koe presentation drafts (intro / roadmap pages). Kept on disk,
# NOT published — sa9saQ/koe is a public repo and the roadmap is internal.
# Promote to tracked docs/ when a public version is intentionally decided.
/docs/koe-intro.html
/docs/koe-roadmap.html
